name: "WINDOWS DOCKURR (FAST noVNC + RANDOM HW PROFILE)"

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  windows-rdp:
    name: "Windows UI Test Node"
    runs-on: ubuntu-latest
    timeout-minutes: 360

    env:
      WINDOWS_USERNAME: "user"
      WINDOWS_PASSWORD: "ngoc0102@"

    steps:
      - name: Prepare workspace and random profiles
        shell: bash
        run: |
          set -Eeuo pipefail
          sudo apt-get update > /dev/null 2>&1
          sudo mkdir -p /mnt/windows_data
          sudo chmod 777 /mnt/windows_data
          mkdir -p oem qemu-wrapper

          # 1. Random RAM
          RAM_OPTIONS=("8G" "12G" "16G")
          RANDOM_RAM=$(shuf -e "${RAM_OPTIONS[@]}" -n 1)

          # 2. SIÊU THỊ ĐIỆN MÁY REAL 100% (Khóa chết Combo: CPU | Hãng | Tên Máy | Hãng BIOS | Ver BIOS | Ngày BIOS)
          HW_PROFILES=(
            # --- ĐỘI DELL ---
            "12th Gen Intel(R) Core(TM) i7-12700H|Dell Inc.|XPS 15 9520|Dell Inc.|1.11.0|11/14/2022"
            "Intel(R) Core(TM) i5-11500 CPU @ 2.70GHz|Dell Inc.|OptiPlex 7090|Dell Inc.|1.2.3|05/12/2022"
            "AMD Ryzen 7 6800H with Radeon Graphics|Alienware|Alienware m15 R7 AMD|Alienware|1.4.2|08/11/2022"
            
            # --- ĐỘI ASUS ---
            "AMD Ryzen 7 6800H with Radeon Graphics|ASUSTeK COMPUTER INC.|ROG Strix G15 G513RM|American Megatrends International, LLC.|G513RM.315|04/16/2022"
            "11th Gen Intel(R) Core(TM) i5-11400H|ASUSTeK COMPUTER INC.|ASUS TUF Gaming F15 FX506HC|American Megatrends International, LLC.|FX506HC.311|11/05/2022"
            "13th Gen Intel(R) Core(TM) i9-13900K|ASUSTeK COMPUTER INC.|ROG Maximus Z790 HERO|American Megatrends International, LLC.|0802|12/23/2022"
            
            # --- ĐỘI HP ---
            "11th Gen Intel(R) Core(TM) i7-1165G7|HP|HP Pavilion Laptop 15-eg0xxx|AMI|F.21|05/18/2022"
            "AMD Ryzen 7 5800X 8-Core Processor|HP|OMEN 30L Desktop GT13-1xxx|AMI|F.11|10/25/2021"
            "Intel(R) Core(TM) i5-10400F CPU @ 2.90GHz|HP|HP ProDesk 400 G7|HPQOEM|2.11|08/01/2023"
            
            # --- ĐỘI LENOVO ---
            "AMD Ryzen 5 5600H with Radeon Graphics|LENOVO|Legion 5 15ACH6|LENOVO|GKCN53WW|05/09/2022"
            "12th Gen Intel(R) Core(TM) i7-1260P|LENOVO|ThinkPad T14 Gen 3|LENOVO|N3MET08W (1.05)|08/16/2022"
            "Intel(R) Core(TM) i5-10400F CPU @ 2.90GHz|LENOVO|IdeaCentre G5 14IMB05|LENOVO|O4NKT19A|12/23/2020"
            
            # --- ĐỘI ACER ---
            "11th Gen Intel(R) Core(TM) i5-11400H|Acer|Nitro AN515-57|Insyde Corp.|V1.19|03/24/2022"
            "AMD Ryzen 7 5800H with Radeon Graphics|Acer|Nitro AN515-45|Insyde Corp.|V1.10|05/06/2021"
            
            # --- ĐỘI MSI ---
            "12th Gen Intel(R) Core(TM) i7-12700H|Micro-Star International Co., Ltd.|Katana GF66 12UC|American Megatrends International, LLC.|E1584IMS.10A|06/15/2022"
            "AMD Ryzen 5 5600X 6-Core Processor|Micro-Star International Co., Ltd.|MAG B550 TOMAHAWK (MS-7C91)|American Megatrends International, LLC.|A.C0|03/22/2023"
            
            # --- ĐỘI GIGABYTE ---
            "13th Gen Intel(R) Core(TM) i5-13600K|Gigabyte Technology Co., Ltd.|Z790 AORUS ELITE AX|American Megatrends International, LLC.|F3|11/15/2022"
            "12th Gen Intel(R) Core(TM) i7-12700H|GIGABYTE|AORUS 15 XE4|American Megatrends International, LLC.|FB05|04/10/2022"
            
            # --- HÀNG ĐỘC LẠ ---
            "12th Gen Intel(R) Core(TM) i7-12800H|Razer|Blade 15 Advanced Model (Early 2022)|American Megatrends International, LLC.|1.06|03/11/2022"
            "11th Gen Intel(R) Core(TM) i7-1185G7|Microsoft Corporation|Surface Laptop 4|Microsoft Corporation|18.102.143|08/25/2022"
          )
          SELECTED_PROFILE=$(shuf -e "${HW_PROFILES[@]}" -n 1)
          
          # Tách mảng PROFILE bằng dấu pipe (|)
          IFS='|' read -r SELECTED_CPU OEM_MANU OEM_PROD BIOS_VENDOR BIOS_VER BIOS_DATE <<< "$SELECTED_PROFILE"

          # Xuất tất cả ra biến môi trường
          {
            echo "RANDOM_RAM=${RANDOM_RAM}"
            echo "SELECTED_CPU=${SELECTED_CPU}"
            echo "OEM_MANU=${OEM_MANU}"
            echo "OEM_PROD=${OEM_PROD}"
            echo "BIOS_VENDOR=${BIOS_VENDOR}"
            echo "BIOS_VER=${BIOS_VER}"
            echo "BIOS_DATE=${BIOS_DATE}"
          } >> "$GITHUB_ENV"

      - name: Generate custom QEMU launcher
        shell: bash
        run: |
          cat > qemu-wrapper/qemu-system-x86_64 <<EOF
          #!/usr/bin/env bash
          set -Eeuo pipefail

          filtered_args=()
          skip_next=0

          for arg in "\$@"; do
            if [[ "\$skip_next" -eq 1 ]]; then
              skip_next=0
              continue
            fi

            if [[ "\$arg" == "-smbios" || "\$arg" == "-cpu" ]]; then
              skip_next=1
              continue
            fi

            filtered_args+=("\$arg")
          done

          exec /usr/bin/qemu-system-x86_64 \\
            "\${filtered_args[@]}" \\
            -smbios "type=0,vendor=${BIOS_VENDOR},version=${BIOS_VER},date=${BIOS_DATE}" \\
            -smbios "type=1,manufacturer=${OEM_MANU},product=${OEM_PROD}" \\
            -cpu "qemu64,model_id=${SELECTED_CPU}"
          EOF

          chmod +x qemu-wrapper/qemu-system-x86_64

      - name: Generate setup-ui.ps1
        shell: bash
        run: |
          cat > oem/setup-ui.ps1 <<'POWERSHELL'
          $ErrorActionPreference = 'SilentlyContinue'

          Remove-Item -Path "HKLM:\SOFTWARE\Microsoft\Windows\CurrentVersion\OEMInformation" -Recurse -Force -ErrorAction SilentlyContinue

          $now = Get-Date
          $monthsBack = Get-Random -Minimum 6 -Maximum 25
          $randomPast = $now.AddMonths(-$monthsBack).AddDays(-(Get-Random -Minimum 0 -Maximum 28))
          $unixTimestamp = [int][Math]::Floor(([DateTimeOffset]$randomPast).ToUnixTimeSeconds())
          Set-ItemProperty -Path "HKLM:\SOFTWARE\Microsoft\Windows NT\CurrentVersion" -Name "InstallDate" -Value $unixTimestamp -Type DWord

          $chars = 'ABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789'.ToCharArray()
          $suffix = -join (1..6 | ForEach-Object { $chars | Get-Random })
          $newName = "DESKTOP-$suffix"
          Rename-Computer -NewName $newName -Force

          $wallpaperDir = "C:\Temp"
          $wallpaperPath = Join-Path $wallpaperDir "wallpaper.jpg"
          New-Item -ItemType Directory -Path $wallpaperDir -Force | Out-Null
          try {
            Invoke-WebRequest -Uri "https://picsum.photos/1920/1080" -OutFile $wallpaperPath -UseBasicParsing
          } catch {}

          if (Test-Path $wallpaperPath) {
            $code = @(
              'using System;'
              'using System.Runtime.InteropServices;'
              'public class Wallpaper {'
              '    [DllImport("user32.dll", SetLastError = true)]'
              '    public static extern bool SystemParametersInfo(int uAction, int uParam, string lpvParam, int fuWinIni);'
              '}'
            ) -join [Environment]::NewLine

            Add-Type -TypeDefinition $code

            Set-ItemProperty -Path "HKCU:\Control Panel\Desktop" -Name WallpaperStyle -Value "10"
            Set-ItemProperty -Path "HKCU:\Control Panel\Desktop" -Name TileWallpaper -Value "0"
            [Wallpaper]::SystemParametersInfo(20, 0, $wallpaperPath, 3) | Out-Null
          }

          $darkMode = Get-Random -Minimum 0 -Maximum 2
          New-Item -Path "HKCU:\SOFTWARE\Microsoft\Windows\CurrentVersion\Themes\Personalize" -Force | Out-Null
          if ($darkMode -eq 1) {
            Set-ItemProperty -Path "HKCU:\SOFTWARE\Microsoft\Windows\CurrentVersion\Themes\Personalize" -Name AppsUseLightTheme -Value 0 -Type DWord
            Set-ItemProperty -Path "HKCU:\SOFTWARE\Microsoft\Windows\CurrentVersion\Themes\Personalize" -Name SystemUsesLightTheme -Value 0 -Type DWord
          } else {
            Set-ItemProperty -Path "HKCU:\SOFTWARE\Microsoft\Windows\CurrentVersion\Themes\Personalize" -Name AppsUseLightTheme -Value 1 -Type DWord
            Set-ItemProperty -Path "HKCU:\SOFTWARE\Microsoft\Windows\CurrentVersion\Themes\Personalize" -Name SystemUsesLightTheme -Value 1 -Type DWord
          }

          $desktop = [Environment]::GetFolderPath("Desktop")
          $folderPool = @("Work", "Projects", "Personal", "Reports", "Archive")
          $shortcutPool = @(
            @{ Name = "VLC";       Target = "C:\Program Files\VideoLAN\VLC\vlc.exe" },
            @{ Name = "7-Zip";     Target = "C:\Program Files\7-Zip\7zFM.exe" },
            @{ Name = "Firefox";   Target = "C:\Program Files\Mozilla Firefox\firefox.exe" },
            @{ Name = "Notepad++"; Target = "C:\Program Files\Notepad++\notepad++.exe" }
          )

          $folderCount = Get-Random -Minimum 2 -Maximum 4
          $selectedFolders = $folderPool | Sort-Object { Get-Random } | Select-Object -First $folderCount
          foreach ($folder in $selectedFolders) {
            New-Item -ItemType Directory -Path (Join-Path $desktop $folder) -Force | Out-Null
          }

          $selectedApps = $shortcutPool | Sort-Object { Get-Random } | Select-Object -First 2
          $wsh = New-Object -ComObject WScript.Shell
          foreach ($app in $selectedApps) {
            $shortcutPath = Join-Path $desktop ($app.Name + ".lnk")
            $shortcut = $wsh.CreateShortcut($shortcutPath)
            if (Test-Path $app.Target) {
              $shortcut.TargetPath = $app.Target
              $shortcut.WorkingDirectory = Split-Path $app.Target -Parent
              $shortcut.IconLocation = $app.Target
            } else {
              $shortcut.TargetPath = "$env:SystemRoot\System32\cmd.exe"
              $shortcut.Arguments = "/c exit"
              $shortcut.WorkingDirectory = "$env:SystemRoot\System32"
              $shortcut.IconLocation = "$env:SystemRoot\System32\shell32.dll,2"
            }
            $shortcut.Description = "$($app.Name) shortcut for UI workspace simulation"
            $shortcut.Save()
          }

          $startupDir = Join-Path $env:APPDATA "Microsoft\Windows\Start Menu\Programs\Startup"
          New-Item -ItemType Directory -Path $startupDir -Force | Out-Null

          $startupCmd = @(
            '@echo off'
            'timeout /t 8 /nobreak >nul'
            'start "" "ms-settings:about"'
            'timeout /t 3 /nobreak >nul'
            'start "" "msedge.exe" "https://tradingradarapp.com/?utm_source=ambas&utm_campaign=urgot&tid=tihon"'
          ) -join [Environment]::NewLine

          Set-Content -Path (Join-Path $startupDir "open-test-page.cmd") -Value $startupCmd -Encoding ASCII
          POWERSHELL

      - name: Generate install.bat
        shell: bash
        run: |
          cat > oem/install.bat <<'BAT'
          @echo off
          setlocal EnableExtensions EnableDelayedExpansion
          powershell -NoProfile -ExecutionPolicy Bypass -File "C:\oem\setup-ui.ps1"
          shutdown /r /t 10
          endlocal
          BAT

      - name: Generate docker-compose
        shell: bash
        run: |
          cat > docker-compose.yml <<EOF
          version: "3.9"
          services:
            windows:
              image: dockurr/windows
              container_name: windows_rdp
              privileged: true
              environment:
                VERSION: "10"
                USERNAME: "${WINDOWS_USERNAME}"
                PASSWORD: "${WINDOWS_PASSWORD}"
                RAM_SIZE: "${RANDOM_RAM}"
                CPU_CORES: "4"
                DISK_SIZE: "128G"
                ARGUMENTS: ""
              devices:
                - /dev/kvm
              ports:
                - "3389:3389"
                - "8006:8006"
              volumes:
                - /mnt/windows_data:/storage
                - ./oem:/oem
                - ./qemu-wrapper/qemu-system-x86_64:/usr/local/bin/qemu-system-x86_64
          EOF

      - name: Boot Windows
        shell: bash
        run: |
          set -Eeuo pipefail
          docker compose up -d > /dev/null 2>&1

      - name: Start tunnels
        shell: bash
        run: |
          set -Eeuo pipefail
          wget -q https://github.com/kami2k1/tunnel/releases/latest/download/kami-tunnel-linux-amd64.tar.gz
          tar -xzf kami-tunnel-linux-amd64.tar.gz > /dev/null 2>&1
          chmod +x kami-tunnel
          rm -f kami_tunnel.txt kami_tunnel_rdp.log kami_tunnel_web.log
          nohup ./kami-tunnel 3389 > kami_tunnel_rdp.log 2>&1 &
          sleep 5
          nohup ./kami-tunnel 8006 > kami_tunnel_web.log 2>&1 &

      - name: Print connection info
        shell: bash
        run: |
          set -Eeuo pipefail

          RDP_IP=""
          RDP_RETRY=0
          while [ -z "$RDP_IP" ] && [ $RDP_RETRY -lt 60 ]; do
            if [ -f kami_tunnel.txt ]; then
              RDP_CONTENT=$(head -1 kami_tunnel.txt 2>/dev/null || true)
              if [[ "${RDP_CONTENT:-}" =~ ^[0-9]+\.[0-9]+\.[0-9]+\.[0-9]+:[0-9]+$ ]]; then
                RDP_IP="$RDP_CONTENT"
                break
              fi
            fi
            sleep 2
            RDP_RETRY=$((RDP_RETRY + 1))
          done

          WEB_IP=""
          WEB_RETRY=0
          while [ -z "$WEB_IP" ] && [ $WEB_RETRY -lt 60 ]; do
            if [ -f kami_tunnel.txt ]; then
              WEB_CONTENT=$(tail -1 kami_tunnel.txt 2>/dev/null || true)
              if [[ "${WEB_CONTENT:-}" =~ ^[0-9]+\.[0-9]+\.[0-9]+\.[0-9]+:[0-9]+$ ]] && [ "$WEB_CONTENT" != "${RDP_IP:-}" ]; then
                WEB_IP="$WEB_CONTENT"
                break
              fi
            fi
            sleep 2
            WEB_RETRY=$((WEB_RETRY + 1))
          done

          if [ -z "${RDP_IP:-}" ]; then
            echo "Khong lay duoc RDP tunnel."
            exit 1
          fi

          if [ -z "${WEB_IP:-}" ]; then
            BASE_IP="${RDP_IP%:*}"
            WEB_IP="${BASE_IP}:8006"
          fi

          echo "RDP  : ${RDP_IP}"
          echo "WEB  : http://${WEB_IP}"
          echo "USER : ${WINDOWS_USERNAME}"
          echo "PASS : ${WINDOWS_PASSWORD}"
          echo "CPU  : ${SELECTED_CPU}"
          echo "OEM  : ${OEM_MANU} ${OEM_PROD}"

      - name: Keep runner alive
        shell: bash
        run: |
          for i in {1..72}; do
            echo "May chu van song ($((i * 5)) phut / 360 phut)"
            sleep 300
          done
